id= "alert-demo-custom-close"
right= true
weight= 3
name= "自定义关闭"
description= "可以自定义关闭，自定义的文字会替换原先的关闭 `Icon`。"
code= """
````jsx
import { Alert } from 'choerodon-ui';

ReactDOM.render(
  <Alert message="Info Text" type="info" closeText="Close Now" />,
  mountNode);
````
"""
script= """
const { Alert } = window['choerodon-ui'];

ReactDOM.render(
  <Alert message="Info Text" type="info" closeText="Close Now" />,
  document.getElementById('alert-demo-custom-close'));
"""