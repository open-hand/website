id= "tooltip-demo-arrow-point-at-center"
weight= 2
left = true
name= "箭头指向"
description= "设置了 `arrowPointAtCenter` 后，箭头将指向目标元素的中心。"
code= """
````jsx
import { Tooltip, Button } from 'choerodon-ui';

ReactDOM.render(
  <div>
    <Tooltip placement="topLeft" title="Prompt Text">
      <Button>Align edge / 边缘对齐</Button>
    </Tooltip>
    <Tooltip placement="topLeft" title="Prompt Text" arrowPointAtCenter>
      <Button>Arrow points to center / 箭头指向中心</Button>
    </Tooltip>
  </div>,
  mountNode);
````
"""
script= """
const { Tooltip, Button } = window['choerodon-ui'];

ReactDOM.render(
  <div>
    <Tooltip placement="topLeft" title="Prompt Text">
      <Button>Align edge / 边缘对齐</Button>
    </Tooltip>
    <Tooltip placement="topLeft" title="Prompt Text" arrowPointAtCenter>
      <Button>Arrow points to center / 箭头指向中心</Button>
    </Tooltip>
  </div>,
  document.getElementById('tooltip-demo-arrow-point-at-center'));
"""