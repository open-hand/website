id= "badge-demo-no-wrapper"
right= true
weight= 1
name= "独立使用"
description= "不包裹任何元素即是独立使用，可自定样式展现。"
code= """
````jsx
import { Badge } from 'choerodon-ui';

ReactDOM.render(
  <div>
    <Badge count={25} />
    <Badge count={4} style={{ backgroundColor: '#fff', color: '#999', boxShadow: '0 0 0 1px #d9d9d9 inset' }} />
    <Badge count={109} style={{ backgroundColor: '#52c41a' }} />
  </div>,
  mountNode);
````
"""
script= """
const { Badge } = window['choerodon-ui'];

ReactDOM.render(
  <div>
    <Badge count={25} />
    <Badge count={4} style={{ backgroundColor: '#fff', color: '#999', boxShadow: '0 0 0 1px #d9d9d9 inset' }} />
    <Badge count={109} style={{ backgroundColor: '#52c41a' }} />
  </div>,
  document.getElementById('badge-demo-no-wrapper'));
"""