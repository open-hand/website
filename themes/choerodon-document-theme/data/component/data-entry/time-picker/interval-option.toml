id= "time-picker-demo-interval-option"
right= true
weight= 3
name= "步长选项"
description= "可以使用 `hourStep` `minuteStep` `secondStep` 按步长展示可选的时分秒。"
code= """
````jsx
import { TimePicker } from 'choerodon-ui';

ReactDOM.render(
  <TimePicker minuteStep={15} secondStep={10} />,
  mountNode);
````
"""
script= """
const { TimePicker } = window['choerodon-ui'];

ReactDOM.render(
  <TimePicker minuteStep={15} secondStep={10} />,
  document.getElementById('time-picker-demo-interval-option'));
"""